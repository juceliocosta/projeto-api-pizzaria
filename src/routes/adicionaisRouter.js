const express = require('express');
const router = express.Router();

// router.get('/adicionais', adicionaisController);
// router.post('/adicionais', criarAdicionalController);
// router.get('/adicionais/:id', obterAdicionalController);
// router.put('/adicionais/:id', atualizarAdicionalController);
// router.delete('/adicionais/:id', excluirAdicionalController);

module.exports = router;